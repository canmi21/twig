// Drizzle schema. Empty by intent — tables land here as the data model
// grows. Add tables with `sqliteTable(...)` and re-run `just database-generate`
// to produce a migration in `database/migrations/`.
//
// Re-export every table from this file so the typed client picks them up.
export {};
