// Namespaced under `anon:` so a later "logged-in display name" under the real
// account system can coexist without key collisions or migration pain.
export const USERNAME_KEY = 'twig:anon:display_name';
